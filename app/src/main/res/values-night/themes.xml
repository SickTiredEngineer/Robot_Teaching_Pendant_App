<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Robot_Teaching_Pendant_App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- DARK Mode Style -->
    <style name = "DARK_BG">
        <item name = "android:background">#5C5A5A</item>
    </style>

    <style name = "DARK_TEXT">
        <item name = "android:textColor">@color/WHITE</item>
    </style>

    <style name = "DARK_BUTTON">
        <item name = "android:textColor">@color/WHITE</item>
        <item name = "android:backgroundTint">@color/MainDark</item>
    </style>

    <style name="Theme.Robot_Teaching_Pendant_App" parent="Base.Theme.Robot_Teaching_Pendant_App" />
</resources>